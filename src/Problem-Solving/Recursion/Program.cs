///When Bigger problem is depend on same type small problem then we have to use Recursion.
///When a funcation calls itself then its called recursion
/// Example : calculate 2^5 = 2 * 2^4 -- Here we can use recursion.
/// 